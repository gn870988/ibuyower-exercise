export type Auction = {
  id: number;
  sellType: string;
  imageUrl: string;
  title: string;
  operatingSystem: string;
  cpu: string;
  displayCard: string;
  drive: string;
  memory: string;
  originalPrice: number;
  specialPrice: number;
  monthlyPayment: number;
  DeliveryDate: string;
  buttonType: string;
};
